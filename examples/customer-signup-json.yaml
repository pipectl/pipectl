id: customer-signup

input:
  format: json

steps:
  - validate-json:
      schema: schemas/signup.json

  - normalize:
      fields:
        email: lower
        name: trim
        string: collapse-spaces

  - redact:
      strategy: mask
      fields: [credit_card, password]

output:
  format: json