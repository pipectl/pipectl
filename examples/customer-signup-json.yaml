id: customer-signup

input:
  format: json

steps:
  - validate-json:
      schema: schemas/signup.json

  - normalize:
      fields:
        email: lower
        name: trim

  - redact:
      fields: [credit_card, password]

output:
  format: json