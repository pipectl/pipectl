id: customer-signup

steps:
  - validate-json:
      schema: schemas/signup.json

  - normalize:
      fields:
        email: lower
        name: trim

  - redact:
      fields: [credit_card, password]